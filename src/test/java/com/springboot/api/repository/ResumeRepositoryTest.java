package com.springboot.api.repository;

/*import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.jdbc.AutoConfigureTestDatabase;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.springboot.api.model.Resume;
@RunWith(SpringRunner.class)
@DataJpaTest
@AutoConfigureTestDatabase(replace=AutoConfigureTestDatabase.Replace.NONE)*/
public class ResumeRepositoryTest {
	
	/*@Autowired
	private ResumeRepository resumeRepositoryMock;

	@Test
	public void testFindByName() throws Exception{
		System.out.println("==Test====");
		Resume resume = new Resume();
		resume.setBatch(2);
		resume.setName("Gagan Vihar");
		resume.setStudy("MBA");
		resume.setSkills("Java");
		resume.setGrade(9.5);
		resume.setCourse("Computers");
		String name = "Gagan Vihar";
		resumeRepositoryMock.findByname(name);
		System.out.println("Resume Details ..."+resume);
		assertEquals(name, "Gagan Vihar");
	}*/

}
