package com.springboot.api.service;

/*import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.springboot.api.model.Resume;
import com.springboot.api.repository.ResumeRepository;

@ExtendWith(MockitoExtension.class)*/
class ResumeServiceTest {
	/*@Mock
	private ResumeRepository resumeRepository1;
	@InjectMocks
	private ResumeServiceImpl resumeService;

	@Autowired
	private ObjectMapper objectMapper;

	@MockBean
	private ResumeRepository resumeRepository;
	@Autowired
	private MockMvc mockMvc;
	private Resume resume;
	
	
	
	@Test
	public void getResumeDetailstest() throws Exception {
		String name = "Gagan Vihar";
		Resume resume = new Resume();
		resume.setBatch(2);
		resume.setName("Gagan Vihar");
		resume.setStudy("MBA");
		resume.setSkills("Java");
		resume.setGrade(9.5);
		resume.setCourse("Computers");
		when(resumeRepository1.findByname(name)).thenReturn(resume);

		Resume response = resumeRepository1.findByname(name);
		assertEquals(name, response.getName());
	}
	@BeforeEach
	public void setup() {
		
		
	}

	@DisplayName("JUnit test for saveResume method")
	@Test
	public void ceateResumeDetailstest() throws Exception {
		String endpointUrl = "/resume/api/resume-details";

		Resume resume = new Resume();
		resume.setBatch(2);
		resume.setName("Gagan Vihar");
		resume.setStudy("MBA");
		resume.setSkills("Java");
		resume.setGrade(9.5);
		resume.setCourse("Computers");
		
		
		
//given(resumeRepository.save(resume));	
		

	


	}*/

}
